import { StoreGenerator } from '@/components/Store/Generate'
import { Container } from '@mantine/core'

export default function Generate() {
	return (
		<Container fluid>
			<StoreGenerator />
		</Container>
	)
}
